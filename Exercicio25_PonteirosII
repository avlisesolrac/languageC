/*
Arquivo Vania Cristina - 2015_2 Pg31 - Slide61

3) Codifique um programa que contenha um vetor de 5 elementos do tipo ponteiro para
um char, insira dados neste vetor e mostre os mesmos.

OBS.: Não esqueça de terminar esse programa quando ter os conceitos que precisa para
concluir esse programa.

*/

# include <stdio.h>
# include <stdlib.h>
# include <string.h>

int main(void){
	int *pontVet = (int*)malloc(sizeof(int));
	char vetorString[5][10];
	
	pontVet = (int*)malloc(sizeof(int));

	for(int i = 0; i < 5; ++i){
		printf("Digite um nome: ");
		pontVet = &vetorInt[i];
		scanf("%s", *vetorInt);
		//pontVet = &vetorInt[i];
	}
	
	for(int i = 0; i < 5; ++i){
		printf("%i\n", pontVet[i]);
	}

	//free(pontVet);
	system("pause");
	return 0;
}
