/*
  Name: Nôno Programa em C
  Copyright: -
  Author: Carlos Eduardo Silva
  Date: 13/09/15 20:07
  Description: Dado um número inteiro n >= 0, calcular o fatorial de n (n!).
*/

# include <stdio.h>
# include <stdlib.h>

int main (void)
{
    int n, fat = 1, aux;
    
    printf("Digite um numero para saber o seu fatorial, maior ou igual a 0: ");
    scanf("%d", &n);
    aux = n;
    while (n > 0)
    {
          fat = fat * n;
          n = n - 1;
    }
    printf("O fatorial do número %d eh: %d\n\n", aux, fat);
    
    system("pause");
    return 0;
}
