/*
  Name: Décimo Quarto Programa em C
  Copyright: -
  Author: Carlos Eduardo Silva
  Date: 13/09/15 22:25
  Description: Dados um número inteiro n > 0, e um dígito d(0<= d <= 9),
  determinar quantas vezes d ocorre em n. Por exemplo, 3 ocorre 2 vezes em 63453
*/

# include <stdio.h>
# include <stdlib.h>

int main (void)
{
	int n, d, aux, cont = 0;
	
	printf("Digite um valor para N: ");
	scanf("%d", &n);
	
	printf("Digite um numero entre 0 e 9: ");
	scanf("%d", &d);
	
	while(n > 0)
	{
		// Ultimo dígito de n;
		aux = n % 10;
	
		if (aux == d)
			cont = cont + 1;
		
		// Arranca o ultimo dígito de n
		n = n / 10;
	}
	
	printf("O número %d apareceu %d vezes.\n\n", d, cont);
	
	system("pause");
	return 0;
}
