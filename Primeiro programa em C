/*
  Name: Primeiro Programa em C.
  Copyright: -
  Author: Carlos Eduardo Silva
  Date: 13/09/15 18:38
  Description: Programa inicial para leitura de uma idade e apresentação na tela
  de uma idade 7 anos mais velho.
*/

# include <stdio.h>
# include <stdlib.h>

int main (void)
{
    /* Primeiro programa em C */
    /* Declarações: todas as variáveis utilizadas precisam ser declaradas. */
    
    int idade;
    
    /* Início do programa. */
    
    printf("Quantos anos voce tem: ");
    scanf("%d", &idade);
    
    printf("%d? Puxa, voce parece que tem %d anos!\n", idade, idade + 7);
    
    /* Fim do programa. */
    
    system("pause");
    return 0;
}
